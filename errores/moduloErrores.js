const manejoErrores = (error, pool, tabla) => {
    let status, message;

    switch (error.code) {
        case value:

            break;

        default:
            status = 500;
            message = "Error genérico del servidor";
            break;
    }
};

export { manejoErrores };